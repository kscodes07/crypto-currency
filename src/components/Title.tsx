import React, { useState } from "react";

function Title() {
  return (
    <div
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "36px",
      }}
    >
      Defi Protocols - TVL Variance
    </div>
  );
}

export default Title;
